def multiply_transpose(c, d) {
  return c;
  # return transpose(c) * transpose(d);
}
# asd
def main() {
  var a = [1, 2, 3];
  var b = [3, 4, 5];
  # var c = [[1, 2, 3], [4, 5, 6]];
  # asd
  # var d<2, 3> = [1, 2, 3, 4, 5, 6];
  var at = transpose(a);
  # var att = transpose(transpose(a));

  return at;
}
